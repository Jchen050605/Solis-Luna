class Event {
    constructor(name, date, bio, pathToImage) {
        this.name = name;
        this.date = date;
        this.bio = bio;
        this.pathToImage = pathToImage;
    }
}

let events = []

function createEvent(name, date, bio, pathToImage) {
    
}